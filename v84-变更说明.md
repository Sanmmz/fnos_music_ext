# fnmusic-ext v1.8.4 变更说明：核心代理容器化（docker-compose 一键拉起）

> 日期：2026-09-22
> 版本：`VERSION` 1.8.3 → 1.8.4
> 目标：把核心代理（接管 `/var/run/trim_music.socket` 的 `fnmusic-ext`）也做成 Docker 容器，
> 实现「一条 `docker compose up` 拉起全部」；同时**保留 systemd 作为一键回退**。
> **fail-closed 安全机制完全不变**（接管/复原逻辑零改动）。

---

## 一、为什么能容器化（关键技术结论）

代理能安全容器化的前提，是接管逻辑依赖「宿主机 PID 命名空间」与「宿主机 `/var/run` 套接字文件」，
这两点现在都能通过容器参数直接复用，无需修改 `takeover.py`：

| 接管机制（`takeover.py`） | 容器化做法 | 是否改动接管逻辑 |
| :--- | :--- | :--- |
| `SO_PEERCRED` + `/proc/<pid>/exe` 校验官方监听身份（fail-closed） | `pid: host` 共享宿主 PID 命名空间，`/proc/<hostpid>/exe` 仍可见 | 否 |
| `renameat2`（no-replace）原子移动宿主机 socket | 挂载 `/var/run:/var/run:rw` | 否 |
| 代理绑定 Unix Socket + 管理台 TCP 8799 | `network_mode: host`，容器内 `127.0.0.1` 直达宿主机音源端口 | 否 |
| `supervise()` 退出时 `state.restore()` 复原官方 socket | 容器 PID 1 = `takeover.py run`（`exec`），`docker stop` 的 SIGTERM 直达 | 否 |
| `supervise()` 硬编码 `<base>/.venv-proxy/bin/python` 启动子进程 | 镜像在 `/app/.venv-proxy` 构建 venv，`--base /app` | 否 |
| `environment()` 强制 `FNMUSIC_HOME=<base>` | 容器内 `FNMUSIC_HOME=/app`，数据目录落在 `/app/{cache,online_favorites,play_history,recommend_cache}` | 否 |

**结论**：原生的 fail-closed 接管与复原路径在容器内与宿主机 systemd 下行为一致，
容器化只是换了「进程包装方式」，安全边界没有弱化。

---

## 二、新增 / 改动文件

### 新增
- `proxy/Dockerfile`：基于 `python:3.13-slim`，在 `/app/.venv-proxy` 构建 venv（满足 `supervise()` 硬编码路径），
  复制 `proxy/` 代码，`CMD` 为入口脚本。pip 用阿里云镜像（fnOS 上清华源返回 403）。
- `proxy/entrypoint.sh`：用 `exec` 把 `takeover.py run --base /app --state-dir /app/.runtime` 设为容器 PID 1，
  确保 `docker stop` 的 SIGTERM 触发 `supervise()` 的 `finally` → 官方 socket 复原。
- `proxy/.dockerignore`：构建镜像时排除 `.venv-proxy` / `__pycache__` / `tests` / `.git` 等。
- `v84-变更说明.md`：本文档。

### 改动
- `docker-compose.yml`：新增 `proxy` 服务（`network_mode: host`、`pid: host`、`/var/run:rw`、
  数据目录与 `music.db` 只读挂载、`depends_on` 三个核心音源健康、`takeover ready` 健康检查、`stop_grace_period: 60s`）。
- `proxy/install_common.sh`：新增 `takeover_docker()` / `proxy_container_running()` / `wait_proxy_healthy()` / `proxy_ready()` 辅助函数。
- `extend.sh`：读取 `FNMUSIC_PROXY_MODE`（默认 `host`）；docker 模式下跳过宿主机 venv 与 preflight、
  幂等性改用容器健康、步骤 3 改为 `docker compose up -d --build proxy`（并 `disable` 但保留 systemd unit）、
  步骤 4 等待容器健康；`rollback()` 支持 docker（停容器即复原）。
- `restore.sh`：自动探测代理运行态（`fnmusic-proxy` 容器在跑 → docker 模式）；docker 模式停容器复原官方 socket、
  **保留 systemd unit 文件**作为回退。
- `.gitignore`：新增 `.runtime/`（接管状态目录）。
- `.env.example`：新增 `FNMUSIC_PROXY_MODE=host` 及说明。
- `VERSION`：1.8.3 → 1.8.4。
- `README.md`：更新两种部署方式对比、删除「代理必须在宿主机 systemd」的旧表述、补充 docker 代理用法。

---

## 三、部署方式

### 默认（host / systemd，作为一键回退）
`.env` 中 `FNMUSIC_PROXY_MODE=host`（默认），行为同 1.8.3：
```bash
sudo ./extend.sh                  # 接管并启用（systemd）
./restore.sh                      # 还原官方直连
```

### 容器化代理（docker）
`.env` 中 `FNMUSIC_PROXY_MODE=docker`，任选其一：
```bash
# 一条命令拉起全部（含 proxy 与 4 个音源）
sudo docker compose up -d --build
# 或仍用一键脚本（会自动停掉 systemd 代理并改用容器接管）
sudo ./extend.sh
```
验证：
```bash
docker ps --filter name=fnmusic-proxy      # 应 Up (healthy)
curl -s --unix-socket /var/run/trim_music.socket http://localhost/_ext/healthz
```

### 回退到 systemd
```bash
./restore.sh                              # 停容器、复原官方 socket，保留 unit 文件
# 之后把 .env 的 FNMUSIC_PROXY_MODE 改回 host，再：
sudo ./extend.sh                          # 重新以 systemd 接管
```

---

## 四、数据安全与风险说明

- **数据持久化**：`cache/`、`online_favorites/`、`play_history/`、`recommend_cache/` 由宿主机目录 bind 挂载进容器，
  历史收藏/播放记录不丢；官方 `music.db` 只读挂载。
- **启动顺序**：`proxy` 依赖 3 个核心音源 `service_healthy` 后才启动；`depends_on` 仅控制顺序，不阻断官方 socket 前置条件
  （需 fnOS【飞牛音乐】应用已运行）。
- **重启冲突防护**：docker 部署时会 `sudo systemctl disable fnmusic-ext`（保留文件），避免重启后 systemd 与容器抢 socket。
- **复原保证**：`docker stop`/`compose down` 均触发 `state.restore()`，官方 socket 在容器退出时被复原；
  若复原失败，`takeover.py` 会报 Unsafe 而非留下半接管状态（与 systemd 行为一致）。
- **失败回滚**：`extend.sh` 任意步骤失败会触发 `rollback()`（docker 模式停容器复原）；`restore.sh` 在停服务前先
  `restore-plan` 预检，不可恢复时不改动任何服务。

---

## 五、已知约束 / 后续可优化

- `proxy` 容器使用 `pid: host` 与 `network_mode: host`，属特权较高的部署形态，仅建议在可信 NAS 上使用（与接管机制本身同源，非新增攻击面）。
- pip 镜像在 Dockerfile 中写死阿里云；如需统一，可加 `PIP_INDEX_URL` build arg（参考音源服务的 `args.FNMUSIC_PIP_INDEX`）。
- 当前 `proxy` 的 `depends_on` 仅含 musicdl/musicbox/lxmusic 三个核心音源（与 `wait_ready` 检查一致）；xmly 为可选音源，不影响接管就绪。
