# v83 变更说明（2026-09-22）

> 对应线上：`~/fnmusic_ext/proxy/app.py`、`proxy/admin_ui.html`
> 验收：NAS 实测 **34/34 通过**（`xm_v82check.py`，其中第 10 项改为校验面板已移除），
> 服务 `active`，`healthz` 正常。
> 版本号：`VERSION` → `1.8.3`

---

## 1. 移除音源页冗余的「音源总开关」面板

**背景**：v82 在「音源」页底部新增了一个「音源总开关」面板，一屏列出全部音源的开关。
但每个音源详情页（点列表进入）本来就各自带一个「启用该音源 / 启用喜马拉雅音源」开关，
二者**指向同一个 env、同一个 `toggleSrc()`**，功能完全重复。用户反馈该面板属于多余
的重复配置，要求删除。

**改动**（仅前端 `admin_ui.html`，后端 `app.py` 不变）：

1. 删除 `renderSourceSwitches()` 函数定义及其在 `renderSources()` 中的调用
   `${renderSourceSwitches()}`。
2. 删除配套的 `.content{overflow-y:auto}` 样式与注释——该样式是为让新增面板在
   矮窗口下不被裁切而加的，面板没了就不再需要，`.content` 恢复上游原本的
   `overflow:hidden`。
3. 同步修订代码内注释（设置页去向说明里「→ 音源页『音源总开关』」改为
   「→ 音源页每个音源的『启用该音源』开关」）。

**能力不丢**：开 / 关某个音源仍可在音源页点进对应音源详情完成，校验项
`/admin/api/config` 读写往返（true→false→true）并落盘 `.env` 仍全绿。

---

## 验收清单（NAS，34/34）

- 第 10 项由「音源总开关已挂载」改为「音源总开关面板已移除（且 `音源总开关` 字样消失）」→ PASS
- v81 回归：本地播放 `Content-Length` 透传、响应体一致（206/206）
- 设置页：`captureScroll` / `restoreScroll` / `#setWrap` 到位
- 重复项：`playlist` 组与 `FNMUSIC_SOURCE_ORDER` 在设置页隐藏（后端 schema 不变）
- 手机端：`.mact` 三处模板、`flex-wrap`、名称区最小宽度、描述可断行
- 每日推荐：描述中文短句、无 `source-native` / `LLM`、`mode_label` 为中文、`mode` 保留
- 接口：`/admin/api/config` 读写往返并落盘 `.env`、`/admin/api/settings/storage`、
  `/admin/api/playlists`、`/healthz`
- 喜马拉雅 2070 集回归（v79）通过；服务 `active`

## 回滚

```bash
cd ~/fnmusic_ext
ls proxy/*.bak.v82-*            # 部署前的 app.py / admin_ui.html
sudo cp <备份> proxy/app.py     # 或 proxy/admin_ui.html
sudo systemctl restart fnmusic-ext
```
