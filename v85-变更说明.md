# v85 变更说明（2026-09-23）

> 版本：1.8.4 → **1.9.0**
> 涉及：`proxy/app.py`、`proxy/admin_ui.html`、`musicbox-service/app.py`、`docker-compose.yml`、README / docs

---

## 1. 综合搜索（歌单 / 小说）大幅加速

**问题**：管理台搜索歌单时经常要等 10~30 秒，偶发 504；小说搜索本身很快（约 0.2s）。

**根因**：歌单搜索慢在网易云链路 —— musicbox 每次搜索都要 spawn 一次 CLI 并打网易接口，
实测冷启动 **2.4~29.3s**，且**没有任何缓存**；代理侧每次也原样重打一遍。

**修复（双层缓存）**：

1. **musicbox 服务侧**：`/api/v1/search` 增加内存 TTL 缓存（默认 600s，上限 300 条，
   `MUSICBOX_SEARCH_CACHE_TTL` / `MUSICBOX_SEARCH_CACHE_MAX` 可调），命中直接返回；
2. **代理聚合侧**：`_admin_playlists_search` 增加结果缓存（默认 600s，
   `FNMUSIC_SEARCH_CACHE_TTL` / `FNMUSIC_SEARCH_CACHE_MAX`），命中时响应带 `cached: true`。

**只缓存成功结果**：任一音源失败或结果为空**不写缓存** —— 否则一次偶发失败会被缓存住，
用户接下来几分钟都看不到网易云。

**实测**：同一关键词第 1 次 2.3s，第 2、3 次 **0.00s**；小说同理。

---

## 2. 搜索失败不再「静默只剩酷我」

- 网易云、酷我改为**并发**执行（总耗时 = 慢的那一个，不再是相加），`ex.map` 保序，
  网易云结果仍排在酷我之前；
- 单源超时 25s → **35s**，失败自动**重试一次**（第二把通常命中缓存）；
- `_mb_http` 超时时返回 `(-1, None, msg)` 而**不抛异常**，旧代码 `return []` 会把失败伪装成
  「网易云没有结果」，重试与告警都被绕过 —— 现在**非 200 一律抛异常**；
- 接口新增 `warnings[]`，管理台在结果下方橙色提示，例如
  「⚠️ 网易云音乐本次搜索失败（TimeoutError）。可再点一次『搜索』重试。」

---

## 3. 手机端「收藏成功但收藏列表没有」已修复

**根因**：`online_history_mode()`（控制在线曲目是否写入收藏 / 播放历史）默认 **`off`**，
且只认 `/app/ONLINE_HISTORY_MODE` 这一个文件 —— 而 `/app` 在容器里**未做持久化挂载**，
容器一重建就丢，也无法在管理台配置。

**表现**：点收藏时代理直接返回 `{"code":0}`（App 提示成功）但**不入库**，收藏列表自然为空。

**修复**：支持环境变量 `FNMUSIC_ONLINE_HISTORY_MODE`（优先级 **env > 文件 > 默认 off**），
写进 `.env`（已挂载）即可长期生效；同时在管理台「配置」页新增该项，可随时开关。
填 `full` 后在线曲目可正常进收藏与播放历史。

---

## 4. 「开了下载却一首都没下载」已修复

两个叠加原因：

1. **下载链路的前置条件没满足**：`fav_dl_on_play`（播放即下载）的触发条件是
   `is_favorite_online_guid(guid)`，而收藏为空（见上一条）⇒ 永远不触发。
   收藏打开后，这条链路自然打通；
2. **落盘目录在容器里写不进去**：飞牛曲库常挂在 **rclone 云盘（fuse）** 上，
   容器里**看得到却写不了** —— 文件能创建、内容 0 字节（`dd` 返回 ENODATA）；
   实测放行 `/dev/fuse` + `SYS_ADMIN` **依然无效**。

**修复**：docker-compose 的 `proxy` 服务新增 `./downloads:/app/downloads:rw`，
把 `.env` 的 `FNMUSIC_TEE_SAVE_DIR` 指向它（容器内 `/app/downloads`）。
部署后如需让飞牛扫到这些音频，把该目录加进飞牛音乐库即可。

**端到端实测**：写入收藏 → 播放触发 → `[favdl] ok ... bytes=32099072` →
宿主机 `downloads/` 出现完整音频 → 自动扫库 `[scanreq] ... status=200 ok=True`。

> 取消收藏即删除同样生效：`iter_media_dirs()` 已包含 `tee_save_dir`，
> 取消收藏时会删除对应音频 / 歌词 / `.ref` 映射并再次触发扫库。

---

## 5. 每日推荐在手机 App 播放失败（404）已修复

- **根因一**：`docker-compose.yml` 只把 `musicbox-data` 挂给了 musicbox 容器，
  **proxy 没挂** ⇒ `_netease_cookie_header()` 恒为 None ⇒ 每首歌都走慢速兜底
  （实测 1.4~17.1s），撞上取流启动预算（4s / 总 12s）⇒ `TimeoutError` ⇒ 404。
  现补上 `- ./musicbox-data:/app/musicbox-data:ro`，直连解析降到 **0.12~0.29s**；
- **根因二**：取流启动预算放宽为 单次 12s / 总 25s（防御性，正常已用不到）。

判据：`/_ext/healthz` 的 `netease_direct.cookie_ok` 应为 `true`、`fallback` 应为 0。

---

## 6. 其它

- **管理台「设置」保存失败**：`.env` 原以 `:ro` 挂载，且 `os.replace` 跨文件系统报 EBUSY；
  改为 rw 挂载 + `os.replace` 失败时回退为直接写入；
- **删除「歌单生成器」功能**：后端移除 `CUSTOM_SOURCE` / `GEN_*` / `_gen_*` /
  `_build_custom_tracks` / `dims` 与 `generate` 接口，前端移除对应面板与样式；
  已下线分组（`ai` / `gen`）的残留选中态自动回落到「综合搜索」；
- **依赖加钉上限**（`starlette<1` 等），避免每次 `docker build` 静默升 major 引爆潜伏 bug；
- **文档收敛为纯 docker-compose**：README 与 `docs/INSTALL.md` 删除「一键脚本 / systemd」
  部署方式，只保留 compose 一条路径。

---

## 升级注意

1. 改完 `.env` 后需 `docker compose up -d --build proxy`（或至少 `restart`）；
2. 若开启收藏（`FNMUSIC_ONLINE_HISTORY_MODE=full`），在线曲目会进入手机端收藏列表；
   若你的客户端对在线条目渲染异常，把它改回 `off` 即可立即恢复原行为。
