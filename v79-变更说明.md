# v79 变更说明：小说《诡秘之主》2070 集只出 1800 集

日期：2026-09-22
影响面：喜马拉雅（xmly）音源的长篇专辑
文件：`xmly-service/app.py`、`proxy/app.py`

---

## 一、现象

把《诡秘之主》（共 **2070** 集）加入歌单后，手机 App 里点开只有 **1800** 集，
末尾 270 集（1793～2070）凭空消失；管理台显示该歌单的集数也是 1800。

---

## 二、根因（两层，缺一不可）

### 1. xmly-service 把翻页写死成 60 页

`/revision/album/getTracksList` 每页固定返回 **30** 条（传 `pageSize` 无效），
而旧代码里：

```python
max_pages: int = Query(60)                    # 默认 60
pages = max(1, min(int(max_pages or 60), 100))  # 上限还是 100，但默认 60
```

`60 × 30 = 1800` —— 正好就是看到的数字。

同时 `proxy._xmly_album_rows()` 的默认参数也是 `max_pages=60` 并且**每次都显式传**，
所以服务端永远只翻 60 页。

实测：《诡秘之主》`trackTotalCount = 2070`，需要 **69 页**；第 61～69 页确实有真实
章节（例如第 69 页首条是「诡秘之主 2031 ‘福生玄黄天尊’」），第 70 页开始为空。

### 2. 磁盘 bundle 缓存把 1800 这个错误数字"固化"了

用户歌单走 `_load_user_bundle()` → 2 小时磁盘缓存（`bundle_cache/<pid>.json`）。
第一次打开时抓到 1800 条就落盘了，之后即使服务端修好，读到的仍是这份 1800 条的旧快照
（TTL 2 小时内不会重拉）。

> 缓存目录属 root，普通 `ls`/`rm` 会 Permission denied，需要 `sudo`。

---

## 三、修复

### xmly-service/app.py

```python
_TRACK_PAGE_SIZE    = 30
_TRACK_MAX_PAGES    = int(os.environ.get("XMLY_TRACK_MAX_PAGES", "200"))   # 200*30 = 6000 集
_TRACK_CONCURRENCY  = max(1, int(os.environ.get("XMLY_TRACK_CONCURRENCY", "8")))
```

- `max_pages` 默认值 `60 → 0`：**0 = 自动**，按第 1 页返回的 `trackTotalCount`
  算需要多少页（`ceil(total/30) + 1`，多翻一页确认结束）；传 `> 0` 仍是显式上限，
  再受 `XMLY_TRACK_MAX_PAGES` 硬顶保护。
- 第 2 页起**并发**抓取（默认 8 并发，`asyncio.Semaphore` 限流），单页失败重试 1 次。
- 结果**严格按 pageNum 升序拼装**，保证章节顺序；遇空页 / 连续失败页即停。
- 拿不到 `trackTotalCount` 时退化为旧的 60 页行为，不会比修复前更差。

### proxy/app.py

```python
async def _xmly_album_rows(album_id, max_pages: int = 0, ...):
    params = {"albumId": aid}
    if max_pages and max_pages > 0:      # 只有显式上限才传
        params["max_pages"] = max(1, int(max_pages))
```

- 默认不再传 `max_pages`，交给服务端按总数自动算。
- 搜索结果封面等场景仍显式传 `max_pages=1`，不会因为这次改动多打 68 次 HTTP。

### 运维动作

清除《诡秘之主》的旧 bundle 缓存快照：

```bash
sudo mv /home/sanmmz/fnmusic_ext/bundle_cache/9df193767d9f.json \
        /home/sanmmz/fnmusic_ext/bundle_cache/9df193767d9f.json.bak.v79
sudo systemctl restart fnmusic-ext
```

（不重启也能靠 2 小时 TTL 自愈，重启只是让它立刻生效。）

---

## 四、验收（11/11 PASS）

| # | 项目 | 结果 |
|---|---|---|
| 1 | xmly-service 直查整张专辑 | 返回 **2070** / total **2070**，**1.11s**（修复前串行 9.9s） |
| 2 | 无重复集 | 重复 **0** |
| 3 | 全部标题非空 | 空标题 **0** |
| 4 | App `size=-1` 拉用户歌单 | http 200，本页 **2070** / total **2070**，**1.56s**，3.77 MB |
| 5 | guid 全属 `online:xmly:*`、无重复 | 重复 0，异常 0 |
| 6 | 全量标题非空 | 空 0 |
| 7 | xmly 直查歌单 guid | 本页 **2070** |
| 8 | 显式 `size=50` 分页 | p1=50 / p2=50 / total=2070，两页**零重叠** |
| 9 | 落盘 `track_count` 自愈 | 1800 → **2070** |
| 10 | — | （当前只有一部小说，未触发） |
| 11 | 歌单列表仍正常 | 共 4 个 |

末条：「诡秘之主小剧场——《愚梦》【这次是真正的结束啦，感恩陪伴❤（都给我哭！）】」
—— 与喜马拉雅专辑末集一致，确认是真·全集。

---

## 五、备份与回滚

| 项目 | 路径 |
|---|---|
| proxy 备份 | `proxy/app.py.bak.v79-20260922-184852` |
| xmly 备份 | `xmly-service/app.py.bak.v79-20260922-184852` |
| bundle 缓存备份 | `bundle_cache/9df193767d9f.json.bak.v79` |

回滚：把两个 `.bak.v79-*` 覆盖回去，重建 xmly 容器并重启代理即可。

---

## 六、可选调参

| 环境变量 | 默认 | 说明 |
|---|---|---|
| `XMLY_TRACK_MAX_PAGES` | `200` | 硬顶页数（200×30 = 6000 集） |
| `XMLY_TRACK_CONCURRENCY` | `8` | 翻页并发数。实测 2070 集：串行 9.9s / 并发 6 → 1.96s / 并发 8 → 1.1s / 并发 10 → 1.38s，**未触发风控** |

---

## 七、经验

1. **"看起来像是分页显示不全"的 bug，先去数服务端的页上限**，不要只盯 App 侧的
   `size`/`page` 解析 —— v78 修了 `size=-1` 的解析，v79 才挖出真正的 60 页天花板。
2. **长列表一定要检查有没有缓存层把旧值固化**：本例服务端修好后 App 仍返回 1800，
   是因为 `_USER_BUNDLE_DISK_TTL = 2h` 的磁盘快照。改完数据链路记得顺手失效缓存。
3. 喜马拉雅 `getTracksList` 的 `pageSize` 参数无效（恒 30 条/页），别试图改它。
