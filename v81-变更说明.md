# v81 变更说明：本地音乐播放失败修复 + 设置页可用性修正

日期：2026-09-22
影响面：所有经插件转发的音频（尤其是本地音乐）+ 管理台设置页
文件：`proxy/app.py`、`proxy/admin_ui.html`

---

## 一、本地音乐播放失败（严重）

### 现象

安装插件后，播放**本地曲库**里的音乐会失败（部分机型表现为能加载但无法播放 / 不能拖动进度）。

### 根因：`forward_to_upstream` 把 `Content-Length` 一起剔除了

```python
# 旧代码
resp_headers = filter_headers(resp.headers,
                              exclude_keys={"content-length", "content-encoding"})
```

插件接管 `/var/run/trim_music.socket` 后，**本地音乐的播放请求也要经过插件转发给上游**
（`guid` 不以 `online:` 开头 → `forward_to_upstream`）。这一剔除让音频响应丢失总长度：

| | 上游直连 | 经插件（修复前） |
|---|---|---|
| `Content-Length` | `11833965` | **（无）** |
| `Transfer-Encoding` | — | `chunked` |
| `Accept-Ranges` | `bytes` | `bytes` |
| `Content-Range`（206 时） | `bytes 0-1023/11833965` | 同左，但**没有长度** |

播放器拿不到总长度就无法确定时长、无法 seek；iOS / 部分安卓播放器会**直接判定播放失败**。

排除 `content-length` 的初衷是「响应体可能被改写」，但这里是**纯透传**（body 原样转发，
且请求已强制 `accept-encoding: identity`），上游给的长度就是真实长度，根本不该删。

### 修复

`proxy/app.py` 两处纯透传（`forward_to_upstream`、`fetch_upstream_envelope`）改为只排除
`content-encoding`，保留 `Content-Length`。

### 验证（A/B 直连上游 socket 对照，24/24）

上游 socket 是 `/var/run/trim_music_upstream.socket`，可直连做对照：

| 场景 | 上游 | 经插件（修复后） |
|---|---|---|
| 整首（无 Range） | 200 / `11833965` | 200 / `11833965` ✓ |
| `Range: bytes=0-1023` | 206 / `1024` | 206 / `1024` ✓ |
| `Range: bytes=1024-` | 206 / `11832941` | 206 / `11832941` ✓ |
| 整首实收字节 | — | `11833965` = 声明长度 ✓ |
| `Accept-Ranges` / `Content-Range` | `bytes` / `bytes …/11833965` | 完全一致 ✓ |

回归：喜马拉雅 2070 集仍为 2070；歌单 / 音源 / healthz / JSON 接口均正常。

> 附：侦查时还在历史日志里看到过 `/music/api/v1/playlist/list` 的 `599 ERR:NameError`，
> 但**当前代码复测返回 200**，判定为历史遗留，已确认不再复现。

---

## 二、设置页底部显示不全

`.content` 是 `height:100vh; display:flex; flex-direction:column; **overflow:hidden**`——
音源页/歌单页靠列内滚动（`.src-col` / `.detail-col` 自带 `overflow-y:auto`）绕开了这个问题，
但设置页是**一整列 6 个分组面板**，超出视口的部分直接被裁掉且滚不到底。

修复：给设置页单独套一层 `.set-wrap`（`flex:1;min-height:0;overflow-y:auto`，
与既有列内滚动同款做法），手机端额外置为 `overflow:visible`（整页滚动，不嵌套滚动区）。

## 三、`U0001F4BE` 是什么 / 已清除

那是 Python 的 8 位 Unicode 转义 `\U0001F4BE`（本意是 💾），被原样写进了 JS 字符串。
**JS 只认 4 位的 `\uXXXX`**，遇到 `\U` 不认识，于是把 `U0001F4BE` 当成普通文字显示出来——
你在界面上看到的这串"乱码"就是它。已改为真实字符，并把其它残留转义（`\u2699`、`\u25B2`）
也一并换成可直接阅读的字符。

## 四、顺带优化设置页

- **分组可折叠**：默认只展开「下载管理」，其余显示标题 + 项数（`N 项`）+ ▼ 展开，
  避免一次面对 40 多项；这也让页面不再需要长距离滚动。
- 折叠按钮整行可点，hover 变品牌色。
- 窄屏：输入框 / 下拉独占一行且高度 ≥44px。

## 五、验收汇总（`xm_v81check.py` 24/24）

| # | 用例 | 结果 |
|---|---|---|
| 1–3 | 本地播放三种 Range 场景：`Content-Length` 透传 / 响应体一致 / `Accept-Ranges`+`Content-Range` 一致 | PASS |
| 4 | 整首实收字节 = 声明的 `Content-Length` | PASS `11833965` |
| 5–9 | 无 `U0001F4BE` / 有滚动容器 / 分组可折叠 / 入口仍在 / 无残留转义 | PASS |
| 10–13 | config / storage / 歌单 / healthz 回归 | PASS |
| 14 | 喜马拉雅 2070 集回归 | PASS |
| 15 | JSON 接口仍可正常解析 | PASS `cl=1413` |

新增 JS 通过 `node --check`（rc=0）。

## 六、备份与回滚

| 项目 | 路径 |
|---|---|
| app.py | `proxy/app.py.bak.v81-20260922-215136` |
| admin_ui.html | `proxy/admin_ui.html.bak.v81-20260922-215136`、`proxy/admin_ui.html.bak.v81b-20260922-215321` |

回滚：覆盖回 `.bak.v81-...` 并 `sudo systemctl restart fnmusic-ext`。
（⚠️ `admin_ui.html` 被 `_ADMIN_UI_HTML` 进程级缓存，改完必须重启才生效。）
