# v86 变更说明（2026-09-23）

本次三项：下载开关去重叠、在线歌单支持移除单曲、远程挂载目录恢复可用。

---

## 1. 下载开关去重叠（设置 → 下载管理）

### 问题

旧版「下载管理」里有 4 个布尔开关，语义严重交叉，用户无法判断该开哪个：

| 旧开关 | 环境变量 | 实际含义 |
| --- | --- | --- |
| 边听边存 | `FNMUSIC_TEE_SAVE_ENABLED` | 播放在线曲目时，边播边把字节流写进曲库 |
| 仅收藏落盘 | `FNMUSIC_TEE_FAVORITES_ONLY` | 把上面那条限制为「只存收藏的」 |
| 收藏即下载 | `FNMUSIC_FAV_DL_ON_FAVORITE` | 收藏那一刻后台整轨下载 |
| 播放即下载 | `FNMUSIC_FAV_DL_ON_PLAY` | 播放已收藏曲目时补漏下载 |

其中「仅收藏落盘」+「收藏即下载」+「播放即下载」三个开关描述的其实是同一件事
（收藏的歌要不要下载），只是触发点不同，组合起来互相打架。

### 新设计：两个正交维度

| 新开关 | 环境变量 | 取值 |
| --- | --- | --- |
| **下载范围** | `FNMUSIC_DOWNLOAD_SCOPE` | `off` / `favorites` / `all` |
| **下载时机** | `FNMUSIC_DOWNLOAD_TRIGGER` | `favorite` / `favorite_play` / `play` |

- **范围**：决定哪些曲目允许落盘
  - `off` —— 不自动下载
  - `favorites` —— 只下载收藏的曲目（推荐）
  - `all` —— 播过的在线曲目全部落盘（占空间，谨慎）
- **时机**：决定什么时候触发整轨下载
  - `favorite` —— 只在收藏那一刻下载
  - `favorite_play` —— 收藏时下载 + 播放已收藏曲目时补漏（推荐，等同旧版两个都开）
  - `play` —— 只要播放就下载，未收藏的也下

两个维度组合唯一，不再有"开了 A 又像开了 B"的情况。

### 兼容性

旧的 4 个环境变量仍然读取。首次升级时若新变量不存在，由旧值自动推导：

- `TEE_SAVE_ENABLED=false` 且两个 fav_dl 都关 → `scope=off`
- `TEE_FAVORITES_ONLY=true`（或只开了 fav_dl_on_favorite）→ `scope=favorites`
- 其余 → `scope=all`
- `on_favorite && on_play` → `trigger=favorite_play`；只 on_play → `play`；否则 → `favorite`

新变量一旦写入 `.env` 即优先。旧的 4 个键从设置页移除（不再展示），但底层仍由新模型统一展开，
避免多处真相不一致。

---

## 2. 在线歌单支持移除单曲

### 问题

手机端在在线歌单（每日推荐 / 我的歌单 / 喜马拉雅专辑）里点「移除」没反应，刷新又回来了。

原因：官方飞牛只认自己库里的歌单。`POST /music/api/v1/playlist/remove-track` 被直接转发给
上游后，上游找不到这个 `online:` 前缀的歌单，静默失败（返回 200 但实际什么都没删）。

### 方案

新增 `POST /music/api/v1/playlist/remove-track` 处理：

- 歌单 guid 属于在线歌单（每日推荐 / 我的歌单 / 心动歌单 / 喜马拉雅）→ **本地记账**：
  把被移除的曲目 guid 记进按歌单维度组织的黑名单
  `online_favorites/playlist_removed.json`，返回 `{"code":0,"data":{"removed":N}}`
- 不属于在线歌单（官方自己的歌单）→ **照旧转发上游**，行为不变

过滤生效在三处，保证列表页计数和实际内容一致：

- `/music/api/v1/track/playlist-detail/list` —— 曲目列表与 `total`
- `/music/api/v1/playlist/detail` —— 歌单详情的 `trackCount`
- `/music/api/v1/playlist/list` 与 `/playlist/batch-detail` —— 列表页显示的曲目数

请求体字段做了兼容解析（`playlistGUID` / `playlistGuid` / `guid` / `playlistId`，
以及 `trackGUIDs` / `trackGuids` / `trackGUID` / `guids` / `tracks` 等），
并支持 JSON body、form 表单与 query 参数三种传法。

### 说明

- 移除只对**在线歌单**生效；官方歌单仍然由飞牛自己管。
- 每日推荐每天重建，移除记录按歌单 guid 保存，跨天不会串。
- 若某天想恢复被移除的曲目，删掉 `online_favorites/playlist_removed.json`
  里对应条目即可（或整个文件删掉）。

---

## 3. 远程挂载目录可以做下载目录了

### 之前的错误结论

上一版把下载目录改到了本地盘 `./downloads`，理由是"曲库挂在 rclone 云盘上，
容器里看得到却写不进去"。这个结论是**错的**，有两个误导来源：

1. **compose 里把 `/vol02` 挂成了 `:ro`** —— 容器内自然是 `Read-only file system`。
2. **用 `dd` 验证** —— `dd` 在这类 fuse（WebDAV via rclone）上会返回
   `0 bytes copied` / `Bad file descriptor`，即使宿主机直接写也一样。这是工具假象。

### 实测结论

| 场景 | 结果 |
| --- | --- |
| 宿主机 `python` 分块写 4MB | 成功，读回一致，2.7s |
| 宿主机 `cp` 真实文件 | 成功 |
| 宿主机 `dd` | 0 字节（假象，不可用 `dd` 判断） |
| 容器（`rw` 挂载）分块写 2MB | 成功，2.5s |
| 真实整轨下载落盘 | 成功，25.9MB 出现在曲库目录 |

### 改动

- `docker-compose.yml`：`- /vol02:/vol02:ro` → `- /vol02:/vol02:rw`
- `.env`：`FNMUSIC_TEE_SAVE_DIR` 指回 `/vol02/<vol-id>/music`

> 注意：云盘写入实测约 1.5MB/s，一首 30MB 约 20 秒。若嫌慢，可在管理台把
> 「下载目录（保存路径）」改成 `/app/downloads`（本地盘，实测 81MB/s），
> 再把该目录加入飞牛音乐库即可。两条路都保留。

---

## 涉及文件

- `proxy/app.py` —— 下载开关推导（`_derive_download_settings` / `_apply_download_derived`）、
  移除单曲接口与过滤、各处 `trackCount` 过滤
- `docker-compose.yml` —— `/vol02` 改 `rw`
- 备份：NAS 上 `proxy/app.py.bak.pre-v86-<时间戳>`、`docker-compose.yml.bak.pre-v86-<时间戳>`
