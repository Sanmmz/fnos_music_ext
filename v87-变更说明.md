# v87 变更说明（2026-09-23）

本次一项：**在线曲目可以加进本地自建歌单了**。

---

## 问题

在飞牛音乐里自己建一个歌单（本地歌单，guid 是飞牛自己生成的 32 位串），
往里加**在线曲目**（网易云 / 酷我 / 洛雪 / 喜马拉雅，guid 带 `online:` 前缀）时：

- 网页端 / 手机端点「添加到歌单」显示成功；
- 进歌单里一看，**空的**；刷新还是空的。

根因和 v86 的「在线歌单移除单曲」是同一类：

> 官方飞牛只认识**自己曲库里**的曲目。歌单表里存的是一条条 `track` 外键，
> 而 `online:netease:3412345` 这种 guid 在官方曲库表里**根本没有对应记录**。
> `POST /music/api/v1/playlist/add-track` 被直接转发给上游后，
> 上游找不到 track，**静默失败**（返回 200，实际什么都没写）。

v86 解决的是「在线歌单 → 移不掉」，v87 补上另一半：「本地歌单 → 加不进」。

---

## 方案

新增 `POST /music/api/v1/playlist/add-track` 处理器，按曲目 guid 分流：

| 曲目类型 | 处理方式 |
| --- | --- |
| 在线曲目（`online:` 前缀） | **本地附加表托管**：把曲目记录写进 `online_favorites/playlist_extra.json`，按歌单 guid 分组 |
| 本地曲目（官方曲库内的 guid） | **照旧转发上游**，行为不变 |
| 两者混选 | 在线部分先落本地附加表，本地部分再转发上游；即使上游因混选整条拒绝，**在线那部分也已入库**，接口仍返回成功，避免界面报错 |

附加表的曲目记录与 `/playlist-detail/list` 里的条目**同构**，读取时直接拼接即可：

```json
{
  "<本地歌单 guid>": [
    { "guid": "online:netease:3401100817", "title": "...", "artist": "...", "...": "..." }
  ]
}
```

写入时按曲目 guid **去重**（重复添加返回 `added: 0`，不会出现两条一样的）。

### 读取侧合并

三处把附加曲目合并进去，保证「点进去能看到」和「列表页数字对得上」：

| 接口 | 合并方式 |
| --- | --- |
| `/music/api/v1/playlist/detail` | 附加曲目追加到曲目列表尾部 |
| `/music/api/v1/track/playlist-detail/list` | 同上，并计入 `total` |
| `/music/api/v1/playlist/list`、`/playlist/batch-detail` | `trackCount` 加上附加曲目数（`_bump_local_playlist_counts`） |

### 移除侧

`POST /music/api/v1/playlist/remove-track` 对本地歌单做了扩展：

- 请求里是**在线曲目** → 从附加表摘掉（`removed: N`）；
- 请求里是**本地曲目** → 转发上游；
- 混选 → 在线部分摘附加表，本地部分转发上游。

这样 v86（在线歌单移除）与 v87（本地歌单增删）在同一个接口上行为自洽。

---

## 兼容性

- 官方歌单里加**本地曲目**：完全走原来的上游路径，行为一字不改。
- 官方歌单的**曲目数**：只有真的加过在线曲目才会被 `+N`，没加过的歌单数值不变。
- 请求体字段名做了兼容解析：歌单 guid 支持 `playlistGUID` / `playlistGuid` / `playlist_guid` /
  `guid` / `playlistId` / `id`；曲目 guid 支持 `trackGUIDs` / `trackGuids` / `track_guid` /
  `trackGUID` / `guids` / `tracks` / `trackIds` / `ids`；并且 **JSON body / form 表单 / query 参数**
  三种传法都能解析。

---

## 使用说明

- 想恢复某个歌单的原始曲目顺序 / 内容：删掉 `online_favorites/playlist_extra.json`
  里对应歌单的条目即可（或整个文件删掉，在线曲目会从所有本地歌单里消失，本地曲目不受影响）。
- 附加表只在**在线曲目**上生效；本地曲目始终由飞牛自己管。

---

## 涉及文件

- `proxy/app.py` —— 新增 `playlist/add-track` 路由与 `_playlist_extra_*` 一族函数、
  `_bump_local_playlist_counts`、`_parse_playlist_write_body`；`playlist/remove-track` 扩展在线曲目分支；
  `playlist/detail`、`track/playlist-detail/list`、`playlist/list`、`playlist/batch-detail` 合并附加曲目。
- 数据文件：`online_favorites/playlist_extra.json`（首次添加时自动创建）。
- NAS 备份：`proxy/app.py.bak.pre-v87-<时间戳>`。
