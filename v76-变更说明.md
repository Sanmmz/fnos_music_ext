# v76 变更说明 —— 喜马拉雅（有声书）音源接入

日期：2026-09-22
机器：`<NAS_IP>`（飞牛 fnOS；`~/fnmusic_ext` 请替换成你自己的部署目录）

---

## 一、需求与用户选定的方案

> 「我想将喜马拉雅作为音源，能够扫码登录喜马拉雅获取 vip 权限 ……
> 主要还是需要能够比较好的实现与目前的项目适配。」

| 决策项 | 用户选择 |
| --- | --- |
| App 里怎么呈现 | **一个歌单 = 一部小说 = 喜马拉雅的一个专辑**；App 搜索框带触发词（如 `小说 星空之门`）时才调用喜马拉雅，不污染常规音乐搜索 |
| 扫码登录入口 | **管理页「音源」页的喜马拉雅卡片** |
| 登录后同步什么 | 暂时只要 **VIP 播放权限**（不同步订阅 / 历史） |

---

## 二、改动的文件

| 文件 | 变化 | 备份 |
| --- | --- | --- |
| `proxy/app.py` | 368045 B → **390344 B** | `proxy/app.py.bak.v76-20260922-160734` |
| `proxy/admin_ui.html` | 55928 B → **62620 B** | `proxy/admin_ui.html.bak.v76-20260922-160734` |
| `docker-compose.yml` | 追加 `xmly` service 块 | `docker-compose.yml.bak.prexmly` |
| `xmly-service/` | **新增** 独立容器（app.py / Dockerfile / requirements.txt） | — |
| `xmly-data/` | **新增** 登录 cookie 落盘卷 | — |
| `.env` | 追加 `FNMUSIC_XMLY_ENABLED='true'`、`FNMUSIC_XMLY_URL='http://127.0.0.1:8774'` | — |

回滚：

```bash
cd ~/fnmusic_ext
cp proxy/app.py.bak.v76-20260922-160734 proxy/app.py
cp proxy/admin_ui.html.bak.v76-20260922-160734 proxy/admin_ui.html
sudo systemctl restart fnmusic-ext
```

---

## 三、架构

```
飞牛音乐 App
     │
     ▼
proxy (takeover /var/run/trim_music.socket)
     │  ── 触发词命中 ──▶ xmly-service (127.0.0.1:8774 → 容器 8000)
     │                        │
     │                        ├─ /revision/search              专辑搜索（无鉴权）
     │                        ├─ /revision/album               专辑详情
     │                        ├─ /revision/album/getTracksList 全部章节
     │                        ├─ /mobile-playpage/…/baseInfo   加密的播放地址
     │                        │     └─ AES-128-ECB 解密 → 真实 m4a 直链
     │                        └─ passport /web/qrCode/*        扫码登录 → cookie 落盘
     │
     └── 未命中触发词 ──▶ 原链路（musicbox / musicdl / lxmusic）完全不变
```

**为什么不把喜马拉雅逻辑塞进 `proxy/app.py`**：签名、AES 解密、cookie 持久化这套东西
和现有三音源的风格完全不同，独立容器可以单独重建/排障，proxy 侧只依赖两个配置项。

---

## 四、喜马拉雅接口实测结论（★ 每项都只有一个能用）

### 1. 搜索 —— 唯一可用且**完全无鉴权**

```
GET https://www.ximalaya.com/revision/search?core=album&kw=<关键词>&page=1&rows=N
  → data.result.response.docs[]  字段：id / title / nickname / cover_path /
                                       intro / play / tracks / is_paid / category_title
```

已排除的路径：

- `/revision/search/main` —— 即使带 xm-sign + 预热 cookie，恒返回
  `{"ret":200,"data":{"reason":"risk invalid","riskLevel":5}}`
- `mobile.ximalaya.com/mobile/v1/search/*` —— 404
- `m.ximalaya.com/m-revision/*` —— 403（WAF）
- `so.ximalaya.com` —— DNS 不通

### 2. 专辑详情 —— 必须走 web

```
GET /revision/album?albumId=<id>
  → mainInfo(albumTitle/cover/shortIntro/playCount/isPaid/vipType)
  + anchorInfo(anchorName)
  + tracksInfo(trackTotalCount, tracks[30])
```

移动端的 `mobile/v1/album/track` 对**约一半专辑**返回 `ret=924 亲，该内容因故已下架`
（实测 32 个专辑只有 13 个可用），所以详情和列表统一走 web。

### 3. 全部章节 —— 路径里**不能带 v1**

```
GET /revision/album/getTracksList?albumId=<id>&pageNum=N&sort=0
```

- 每页**固定 30 条**（传 `pageSize` 无效），翻到空为止
- 实测 1024 集专辑能完整拉到 1024 条（35 页）
- `/revision/album/v1/getTracksList` 要 webtk cookie → `407 webtk缺失`，
  且第 2 页起 `401 未登录用户仅允许访问第一页`

### 4. 播放地址 —— 需 AES 解密

```
GET /mobile-playpage/track/v3/baseInfo/{albumId}?device=web&trackId={tid}&trackQualityLevel=1
  → trackInfo.playUrlList[0].url    ← 加密字符串

解密：AES-128-ECB，密钥 = bytes.fromhex("aaad3e4fd540b0f79dca95606e72bf93")
      base64.urlsafe_b64decode(cipher + "==") → 去 PKCS7 填充
      ⇒ https://a.xmcdn.com/storages/…m4a?sign=…&buy_key=…&timestamp=…
```

### 5. xm-sign —— 这条链路**不需要**

社区项目普遍把它当作"绕过调用限制的关键"，算法是：

```
md5("himalaya-" + serverTime) + "(" + rand + ")" + serverTime + "(" + rand + ")" + nowTimeMs
```

（`serverTime` 取自仍活着的 `/revision/time`）

但实测：最关键的那条搜索链路 `/revision/search` 带签名反而更容易触发风控。
**不要为了签名而签名** —— 先把无鉴权路径试完。

---

## 五、proxy 侧接入点（10 处）

| 位置 | 作用 |
| --- | --- |
| `CONF` | 新增 `xmly_url` / `xmly_enabled` |
| `_source_enabled` | **必须单独加 `xmly`**，否则会掉进 `musicdl_enabled` + `online_sources` 校验，被判成「已停用音源」只返回 retained ⇒ 空标题 ⇒ 手机端整列不渲染（v29 的血案） |
| `search_track` 开头 | 触发词劫持，直接 `return await _xmly_search_track_response(...)` |
| `search/playlist` 路由 | 触发词时把专辑当歌单返回（App 实际会大量调用） |
| `playlist_detail` | `is_xmly_playlist_guid` 早返回 |
| `playlist_batch_detail` | `xmly_ids` 并入 `local_ids`，`asyncio.gather` 并发组装 |
| `playlist_track_list` | 整张专辑一次性给全，支持 `page` / `size` 分页 |
| `_open_online_stream` | 新增 `xmly` 分支，直接下发已解密直链 |
| `_fetch_online_info` | 新增 `xmly` 分支，优先取专辑缓存（零请求） |
| `_admin_sources_payload` + `ADMIN_GROUPS` | 管理卡片 + 开关 schema |

### guid 设计

```
歌单（专辑）：online:playlist:xmly:<albumId>
单集（声音）：online:xmly:<trackId>:<albumId>
```

第二条让 `source_from_online_guid()`（`parts[1]`）自然给出 `"xmly"`，
于是 `build_online_track` / `online_sources` / `_open_online_stream` 全部复用现有框架，
不需要为喜马拉雅写专属分支。

对应的 item 里 `id` 写成 `xmly:<tid>:<aid>` —— `online_guid_from_item()` 见到含 `:` 的 id
会拼成 `online:<id>`，所以 **id 不能再带 `online:` 前缀**。

### ★★ 性能坑：别把一次搜索变成几十次 HTTP

一部小说动辄上千集，而搜索结果只需要第一集来"代表这部小说"：

```python
details = await asyncio.gather(*[_xmly_album_rows(a["albumId"], max_pages=1,
                                                  cache_only_first_page=True)
                                 for a in albums], return_exceptions=True)
```

完整拉满只发生在用户真正点开歌单时。
实测：`小说 吞噬星空` 20 条结果 **1.12 s**；1305 集歌单第 1 页（50 条）**3.35 s**。

---

## 六、管理页

- 左侧「音源」新增「🎙️ 喜马拉雅（有声书）」卡片，**固定在列表末尾**，不参与常规音乐搜索的
  优先级拖拽排序 —— 它是触发词专用音源，写进 `FNMUSIC_SOURCE_ORDER` 会拖慢正常搜索并挤占结果位。
- 卡片徽标：`VIP 已登录` / `已登录` / `未登录` / `不可达` / `已关闭`。
- 未登录时显示「扫码登录」按钮；二维码是 base64 PNG，2 秒轮询一次
  （`pending → scanned → success / expired`），成功后自动刷新状态。
- 支持「重新扫码登录」「退出登录」「启用/停用」。
- 新接口：`/admin/api/xmly/status`、`/admin/api/xmly/qr`、`/admin/api/xmly/qr/check`、
  `POST /admin/api/xmly/logout`（都在 8799 端口）。

---

## 七、怎么用（给使用者）

在飞牛音乐 App 的搜索框输入：

```
小说 吞噬星空
```

- 「歌曲」维度：每行 = 一部小说，点开直接播第 1 集
- 「歌单」维度：每行 = 一部小说 = 一个歌单，点开就是完整章节列表（支持翻页）

触发词有三个：`小说`、`喜马拉雅`、`xmly`（大小写均可），后面留一个空格再写书名。
**不带触发词时完全走原来的网易云 / 洛雪 / 音乐猫链路，行为与 v75e 完全一致。**

- 未登录：能搜、能听免费章节
- 要听 VIP / 付费专辑：到 `http://<NAS_IP>:8799/admin` → 左侧「音源」→ 喜马拉雅 →
  **扫码登录**（用喜马拉雅 App 扫码）

---

## 八、验收结果

`xm_e2e.py`：**14 / 16 PASS**。两个 FAIL 经定点复核后确认为**验收脚本自身的问题**，
功能实际全部可用（见下）。

| # | 用例 | 结果 |
| --- | --- | --- |
| 1 | `小说 吞噬星空` 搜索只返回 xmly | PASS，20 条全 `source=xmly`，1.12 s |
| 1b | 标题非空（App 整列消失的根因防线） | PASS，empty=0 |
| 1c | 封面已回填（不用等 `/static/cover`） | PASS，无封面=0 |
| 2 | 不带触发词时不劫持 | PASS |
| 3 | 歌单搜索返回喜马拉雅专辑 | PASS，10 个（1305 / 1024 / 1645 / 746 / 1308 集…） |
| 4 | 歌单详情 | PASS，名称 + 集数 + 封面齐全 |
| 5 | 歌单曲目第 1 页 | PASS，50 条 / total=1305，3.35 s |
| 5b | 全部标题非空 | PASS |
| 5c | 曲目 guid 均归属 xmly | PASS |
| 6 | 第 2 页分页 | PASS，首条 = 第 50 集 |
| 7 | 播放链路 | 见下方说明，**实际 PASS** |
| 8 | 批量歌单详情 | 见下方说明，**实际 PASS** |
| 9 | 管理页 xmly 状态接口 | PASS |
| 10 | 音源面板含 xmly 且已启用 | PASS，`health=ok` |
| 11 | 扫码二维码 | PASS，qrId 正常，PNG base64 约 4.8 KB |
| 12 | 扫码轮询接口 | PASS，`status=pending` |

### 两个"假 FAIL"的说明

**① 播放链路** —— 首次用 `HEAD` 测得到 `200 text/html`，看着像失败。
真因：**Starlette 的 `@app.get` 不响应 `HEAD`**，请求落到了 SPA fallback。
改用 `GET + Range: bytes=0-2047` 复测：

```
status=206  耗时 0.22s
content-type   = audio/mp4
content-length = 2048
content-range  = bytes 0-2047/2844760
accept-ranges  = bytes
body = b'\x00\x00\x00\x1cftypM4A \x00\x00\x02\x00M4A isomiso2…'   ← 真实 M4A 文件头
```

全量 GET 也正常：`200 audio/mp4`，`content-length: 2844760`。

**② 批量歌单详情** —— 返回 `401 INVALID TOKEN`。对照组实测：

```
xmly            -> 401 {"code":99999,"msg":"INVALID TOKEN"}
daily(对照组)     -> 401 {"code":99999,"msg":"INVALID TOKEN"}
unknown(对照组)   -> 401 {"code":99999,"msg":"INVALID TOKEN"}
```

这是 `_probe_upstream_auth` 的**既有行为**（验收脚本没带 App 令牌），不是本次引入的回归。

### 其他硬证据

```
# 容器侧直链 + 源站
track/url → ok=True url=https://audiopay.cos.tx.xmcdn.com/download/1.0.0/storages/…m4a?sign=…
origin HEAD → 200 ct=audio/x-m4a len=2844760

# 服务
fnmusic-xmly   Up 40 minutes (healthy) | 127.0.0.1:8774->8000/tcp
healthz=200
journalctl -u fnmusic-ext --since "-25min"  → 无异常（唯一 traceback 来自测试脚本提前断连）
```

---

## 九、本次踩到的坑（已写进 skill）

1. **Starlette 的 `@app.get` 不响应 `HEAD`** —— 验收 `/track/stream` 必须用
   `GET + Range: bytes=0-2047`，否则拿到 `200 text/html` 的假阴性。
2. **`http.client` 自己会发 `Host` 头** —— 手写 `putrequest` 又 `putheader("Host")`
   会发出两个 Host ⇒ uvicorn 直接 `Invalid HTTP request received.`（400）。
   改用 `c.request(...)`，或干脆用 `curl --unix-socket`。
3. **`playlist/batch-detail` 在无 token 时 401 是既有行为** —— 做 E2E 必须跑对照组
   （daily / 乱填 guid 一起试）才能下结论。
4. **往 `do_POST` 里插分支最容易顶掉上一块的 body** —— 本次就把
   `if path == "/admin/api/netease/qr":` 的 body 顶没了，`py_compile` 报 `IndentationError`。
   改完务必先编译，且要用 `cfile=/tmp/…` —— `proxy/__pycache__` 可能不可写，
   否则会把**权限错误误判成语法错误**。
5. Windows PowerShell 控制台显示远端 UTF-8 中文是乱码 —— 让脚本把报告写成
   `/tmp/xxx.txt` 再 scp 回来读。

---

## 十、遗留 / 已知限制

- VIP 专辑的实际拉取**尚未用真实 VIP 账号验证**（需要用户扫码）。已验证的是：
  匿名态下免费的 1305 集专辑能正常搜索 / 列表 / 播放。
- 本次**未**同步订阅、收藏、收听历史（按用户选择，暂时不做）。
- `_probe_upstream_auth` 会让不带 App 令牌的调用返回 401（既有行为，非本次引入）。
- `playlist/batch-detail` 仍要求 App 令牌，与每日推荐/用户歌单行为一致。
