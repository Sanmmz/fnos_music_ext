# v80 变更说明：管理台新增「设置」页（收藏下载 / 取消收藏删除 / 播放下载 / 下载目录）

日期：2026-09-22
影响面：Web 管理控制台 + 后台下载行为
文件：`proxy/admin_ui.html`、`proxy/app.py`

---

## 一、需求

1. 向导栏（侧边栏）增加「设置」入口；
2. 设置界面提供 **收藏下载开关 / 取消收藏删除开关 / 播放下载开关 / 下载目录设置**。

## 二、关键前提：能力已经在，缺的是入口

排查后发现，后端其实早就实现了这四件事，`/admin/api/config` 的 schema 里也都有对应项，
只是管理台（此前只有「音源」「歌单」两页）**从未渲染它们**，用户只能通过手改 `.env` 来开启。
所以这次不是重写功能，而是**把已有能力正确地接上 UI**，并补上「目录到底有没有生效」的反馈。

| 需求 | 环境变量 | conf 键 | 默认 | 改动前 UI |
|---|---|---|---|---|
| 收藏下载开关 | `FNMUSIC_FAV_DL_ON_FAVORITE` | `fav_dl_on_favorite` | `true` | 无 |
| 取消收藏删除开关 | `FNMUSIC_FAV_DELETE_ON_UNFAV` | `fav_dl_delete_on_unfav` | `true` | 无 |
| 播放下载开关 | `FNMUSIC_FAV_DL_ON_PLAY` | `fav_dl_on_play` | `true` | 无 |
| 下载目录 | `FNMUSIC_TEE_SAVE_DIR` | `tee_save_dir` | 空（自动探测曲库） | 无 |

三者触发链路（均为既有代码，本次未改）：

- **收藏**：`POST /music/api/v1/favorite-track/create` → 落 `online_favorites/*.json`
  → `CONF["fav_dl_on_favorite"]` 为真则 `spawn_favorite_download()` 后台整轨下载；
- **取消收藏**：`POST /music/api/v1/favorite-track/delete` → 若已不在任何收藏列表且
  `CONF["fav_dl_delete_on_unfav"]` 为真，删除对应音频 / 歌词 sidecar / 引用；
- **播放**：`GET /music/api/v1/track/stream` → 当 Range 从头开始且是**已收藏**曲目，
  `CONF["fav_dl_on_play"]` 为真则触发整轨下载（解决「播放器按 1MB 窗口取流，tee 尾部落盘永远轮不到」）。

> 另：「边听边存」(`FNMUSIC_TEE_SAVE_ENABLED`) 是**任意**在线曲目边播边存，与上面的
> 「播放即下载（仅限收藏曲目）」是两件事，两个开关都已在设置页暴露，可按需要只开其一。

## 三、改动

### `proxy/app.py`

1. 新增 `_admin_storage_info()` + `GET /admin/api/settings/storage`
   （同时挂在 FastAPI `/music/api/v1/admin/api/...` 前缀和独立 TCP 端口 `8799` 下）。
   返回：配置值 / **实际生效值** / 是否发生回退 / 目录是否存在 / 是否可写 /
   磁盘可用与总量 / 目录内音频文件数与占用。

   为什么必须加：`tee_save_dir()` 在目录不可写时会**静默回退**到自动探测的曲库目录，
   用户填了路径却不知道没生效 —— 现在设置页能立刻看出来。

2. 下载目录的一项文案从「边听边存保存路径」改为「**下载目录（保存路径）**」，
   说明三种下载都落到这里、保存即生效。

3. ★ 磁盘余量加了**可信度判断**：曲库若挂在 rclone / WebDAV 等云盘上，`statvfs`
   会把容量报成 1PB 且完全无已用。超过 512TB 或 `free > total` 一律标记为
   `space_unknown`，界面显示「可用空间未知（网络盘 / 云挂载）」而不是 1048576 GB。

### `proxy/admin_ui.html`

1. 侧边栏新增 **⚙️ 设置** 入口（`setView('settings')`），手机端自动变成第三个底部 Tab。
2. 设置页**完全由后端 schema 驱动**：拉 `GET /admin/api/config` 渲染分组与控件，
   新增 schema 项无需再改前端。支持 bool（开关）/ enum（下拉）/ text / int / float /
   MB / secret（密码框）全部类型。
3. 「下载管理」分组置顶，且组内把这次要的 4 项（再加边听边存）排在最前面。
4. 顶部新增 **下载目录** 实时状态卡：生效路径、可写徽标、可用空间、已有音频数量；
   配置了路径就显示「已配置路径」，不可写时明确提示**已静默回退**；留空则显示
   探测到的曲库并提供「填入探测到的曲库」按钮。
5. 改动即时写入 `.env` 并 `_apply_live()` 生效（这四项都**不需要重启**）；
   标记 `restart` 的项显示「重启生效」徽标，并在顶部给出「立即重启」按钮。
6. 窄屏适配：`padding-bottom` 预留底部 Tab 高度，长输入框独占一行，控件高度 ≥44px。

> ⚠️ `admin_ui.html` 被 `_ADMIN_UI_HTML` 做进程级缓存，**改完必须重启 fnmusic-ext** 才生效。

## 四、验收（`xm_v80check.py` 19/19）

| # | 用例 | 结果 |
|---|---|---|
| 1–3 | 管理台含「设置」入口 / 设置页 JS / 原页面入口仍在 | PASS |
| 4–5 | `/admin/api/config` 可读且四项都在 schema | PASS（标签：收藏即下载 / 取消收藏即删除 / 播放即下载 / 下载目录（保存路径）） |
| 6–7 | `/admin/api/settings/storage` 返回生效目录与可写性 | PASS 生效=`/vol02/<vol-id>/music` 可写=true |
| 7b | 云挂载不再显示虚假余量 | PASS 标记 `space_unknown` |
| 8–10 | 三项开关来回切换、**持久化到 `.env`**、再还原 | PASS |
| 11 | 设置下载目录后立即生效（目录自动创建） | PASS `/tmp/_v80_dltest` |
| 12 | 非法目录（`/proc/...`）被识别并回退 | PASS `fallback=true`，生效目录仍是曲库 |
| 13 | 下载目录还原为原值 | PASS |
| 14–16 | 歌单 / 音源 / healthz 既有接口无回归 | PASS |

附加检查：新增 JS 通过 `node --check`（0 错误）。

## 五、备份与回滚

| 项目 | 路径 |
|---|---|
| app.py 备份 | `proxy/app.py.bak.v80-20260922-210328`、`proxy/app.py.bak.v80b-20260922-211011` |
| html 备份 | `proxy/admin_ui.html.bak.v80-20260922-210328`、`proxy/admin_ui.html.bak.v80b-20260922-211011` |

回滚：把 `.bak.v80-...`（或更早的 `.bak.v79-...`）覆盖回同名文件，重启 `fnmusic-ext` 即可。

## 六、怎么用

浏览器打开 `http://<NAS_IP>:8799/admin` → 点左侧 **⚙️ 设置**：

- 「下载管理」分组最上面就是 **收藏即下载 / 取消收藏即删除 / 播放即下载** 三个开关；
- 紧跟着是 **下载目录（保存路径）**：留空＝自动用飞牛共享曲库，填了以填写为准；
- 改完立刻生效，顶部的「下载目录」卡会同步显示实际写到哪、能不能写。
