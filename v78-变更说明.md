# v78 变更说明 —— 小说歌单打开后只剩 49/50 集（分页语义 bug）

日期：2026-09-22
机器：飞牛 fnOS（插件目录 `~/fnmusic_ext`，请替换成你自己的部署目录）

---

## 一、现象

搜索「小说 三体」→ 把《三体》专辑加入歌单 → 在 App 里点开这个歌单，
**只显示 49 集**（或 50 集），而专辑实际有 **313 集**。翻不到底，后面的章节全部消失。

---

## 二、根因：`size=-1` 被兜底成 50，导致"一次给全"的分支成了死代码

翻 `access_probe.log` 找到 App 的真实请求：

```
GET /music/api/v1/track/playlist-detail/list?playlistGUID=...&page=1&size=-1&sort=&lan=zh-CN
```

`-1` 的语义是**「不分页，一次给全」**。但老代码的处理顺序是：

```python
# 老代码
size = int(request.query_params.get("size") or 50)
if size < 1:          # ← -1 在这里被兜成 50
    size = 50
...
if size != -1:        # ← 到这里 size 永远 >= 1，这个分支永远进不去（死代码）
    items = items[start:start + size]
```

于是 313 集的专辑永远只吐前 50 集。
**用 `page=1&size=50` 一页页翻是能翻完的，但 App 根本不翻页——它只发一次 `size=-1`。**

> ★ 排障方法：不要猜客户端传什么，直接翻 `access_probe.log`，App / 网页的真实
> query string 都记在里面。本次就是靠日志里那行 `size=-1` 一击定位的。

---

## 三、修复

在 `track/playlist-detail/list` 路由前新增两个工具函数：

```python
# 歌单曲目分页：客户端（手机 App / 网页）打开歌单时传的是 `page=1&size=-1`，
# `-1` 的语义是「不分页，一次给全」。以前 `if size < 1: size = 50` 把它兜成 50
# ⇒ 上千集的小说只显示前 50 集（v78 修）。
_PAGE_ALL_CAP = 5000

def _page_size(request) -> int:
    """返回分页大小；`<=0`（含 App 的 -1）表示「一次给全」。"""
    raw = request.query_params.get("size")
    if raw is None or raw == "":
        return 50
    try:
        return int(raw)
    except (TypeError, ValueError):
        return 50

def _slice_page(items: list, page: int, size: int) -> list:
    """按 page/size 切片；size <= 0 时返回整张（受 _PAGE_ALL_CAP 保护）。"""
    if size <= 0:
        return list(items or [])[:_PAGE_ALL_CAP] if page <= 1 else []
    start = (page - 1) * size
    return list(items or [])[start:start + size]
```

改动点只有两处：

| 位置 | 改动 |
|---|---|
| `playlist_track_list` 的 **xmly 分支** | `size = _page_size(request)` → `page_tracks = _slice_page(all_tracks, page, size)` |
| `playlist_track_list` 的 **通用分支**（用户歌单 / 每日推荐 / 心动） | 同上，改用 `tracks` |

设计取舍：

- **`size <= 0` ⇒ 一次给全**，但用 `_PAGE_ALL_CAP = 5000` 兜底，防止异常大的歌单
  把响应体撑爆（实测 313 集 = 535 KB / 2.3 s，完全可接受）。
- **`page > 1` 且 `size <= 0` ⇒ 返回空**（"一次给全"没有第 2 页的概念）。
- **`size` 缺失 / 非数字 ⇒ 50**，与老行为一致，不影响任何既有调用方。

---

## 四、验收（`xm_v78check.py`，8 / 9）

| # | 用例 | 结果 |
|---|---|---|
| 1 | `size=-1` 一次给全（用户歌单） | **PASS** `http=200 本页=313 total=313 2.30s len=535513B` |
| 2 | `xmly` 直查 `size=-1` | **PASS** `本页=313 total=313` |
| 3 | 显式 `size=50` 分页不受影响 | FAIL（**断言写错了**，见下） |
| 4 | 分页之间不重复 | **PASS** `重叠=0` |
| 5 | 全量标题非空（App 整列消失防线） | **PASS** `empty=0` |
| 6 | guid 全部归属 `online:xmly:*` | **PASS** |
| 7 | 无重复 guid | **PASS** |
| 8 | 服务与容器健康 | **PASS** `fnmusic-ext active`，4 个音源容器 Up |
| 9 | 每日推荐 `size=-1` 也一次给全 | **PASS** `本页=20 total=20` |

### 关于第 3 条的"假 FAIL"

断言写成了「第 1 页条数 ≠ 第 2 页条数」，但 313 集的专辑用 `size=50` 分页时，
第 1 页和第 2 页都是**恰好 50 条**，长度当然相等。
真正该比的是**内容**：第 4 条「分页之间不重复，重叠 = 0」已经证明分页是正确的。
功能没问题，是断言写窄了。

---

## 五、回滚

```bash
cd ~/fnmusic_ext
sudo cp -a proxy/app.py.bak.v78-20260922-174410 proxy/app.py
sudo systemctl restart fnmusic-ext
sudo systemctl is-active fnmusic-ext
```

> 上一层备份是 `proxy/app.py.bak.v77b-<TS>`（v77b，本次修改前的状态）。

---

## 六、当前线上状态

- `proxy/app.py` **401 398 B**（v78；v77b = 400 308 B）
- 备份：`proxy/app.py.bak.v78-20260922-174410`
- 编译检查 `COMPILE_OK`，服务 `fnmusic-ext`：**active**
- 容器：`fnmusic-musicbox` / `fnmusic-musicdl` / `fnmusic-lxmusic` / `fnmusic-xmly` 全部 Up (healthy)

---

## 七、坑位沉淀

1. **`size` 的兜底顺序会制造死分支**：先 `if size < 1: size = 50` 再判 `size == -1`，
   后者永远不成立。凡是"约定值"（`-1` / `0` / `all`）都要在兜底**之前**处理。
2. **客户端发什么参数，去 `access_probe.log` 看**，不要靠猜。App 的 `size=-1`
   和网页的 `size=50` 可能完全不同。
3. **"一次给全"要加容量上限**：`_PAGE_ALL_CAP = 5000`。否则一个几万条的异常歌单
   会把响应体撑到几十 MB，手机端直接超时白屏。
