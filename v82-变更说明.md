# v82 变更说明（2026-09-22）

> 对应线上：`~/fnmusic_ext/proxy/app.py`、`proxy/admin_ui.html`
> 验收：NAS 实测 **34/34 通过**（`xm_v82check.py`），服务 `active`，`healthz` 正常。

---

## 1. 设置页点击「展开」跳回顶部

**现象**：在设置页滚动到下面某个分组，点标题展开/收起，页面立刻弹回顶部。

**根因**：`toggleGroup()` 改完 `STATE.settingsOpen` 后直接调 `render()`，
`renderSettings()` 是 `c.innerHTML = html` 整块替换，滚动容器被销毁重建 ⇒
`scrollTop` 归零（手机端则是窗口滚动位置丢失）。

**修法**：给 `render()` 套上滚动位置存取——

```js
function render(){
  const _k = captureScroll();          // 记录 #content / #setWrap / #detailCol / window
  if(STATE.view==="playlist"){ renderPlaylists(); restoreScroll(_k); return; }
  if(STATE.view==="settings") renderSettings(); else renderSources();
  restoreScroll(_k);                   // 重绘后原样写回
}
```

顺带把设置页的滚动容器加上 `id="setWrap"`，让位置能精确定位。
这一步对**所有**页面生效：保存某项配置后触发的 `loadSettings() → render()`
同样不再跳顶。

---

## 2. 与其他页面重复的设置项 → 合并到对应页面

设置页里有两组东西在别的页面已经能改，重复出现容易「改了这边以为没生效」：

| 原在设置页 | 与谁重复 | 现在 |
|---|---|---|
| **歌单管理**组的 4 个音源开关（网易云 / 洛雪 / 音乐猫 / 喜马拉雅） | 音源页每个音源详情里的「启用该音源」 | 合并到**音源页**新增的「音源总开关」面板 |
| **FNMUSIC_SOURCE_ORDER**（音源搜索优先顺序） | 音源页列表右侧 `⋮⋮` 拖动排序 | 去掉输入框，拖动即写入 |

- 音源页底部新增「**音源总开关**」面板：一屏开关全部音源，附当前状态
  （已连接 / 未登录 / 已关闭 …），与逐个音源页共用同一个 env，两处实时一致。
- 设置页顶部加了一句去向说明，避免找不到开关。
- 过滤是 **UI 侧**（`SETTINGS_HIDDEN_GROUPS` / `SETTINGS_HIDDEN_ITEMS`），
  后端 schema 与 `/admin/api/config` 读写接口**完全不变**，脚本/自动化不受影响。
- 分组过滤后若某组已空（如被隐藏的 playlist 组）自动不渲染。

---

## 3. 手机端歌单行「显示很奇怪」

**现象**：web 端（桌面浏览器）歌单页比例正常，手机端歌单名称/描述被挤成一条细缝，
尤其「每日生成推荐歌单；模式：source-native；LLM 兜底：关。」显示很怪。

**根因**（两件事叠加）：

1. `.pl-manage-row` / `.pl-src-row` 是**一行 flex**：
   `拖动把手(44) + 图标(30) + 名称描述 + 开关 + [刷新][删除]`，
   375px 手机上留给「名称 + 描述」的只剩约 **27px**，文字被压成竖条。
2. 那句描述本身**中英混排 + 全角分号**，`source-native` 里的连字符、
   句末「。」在窄行里的断行位置都很别扭。

**修法**：

1. 行尾操作区统一包进 `<div class="mact">`（当前歌单 / 音源歌单 / 综合搜索 三处），
   手机端 `flex-wrap:wrap` 时整体换到第二行右对齐，名称描述占满整行：

   ```css
   @media(max-width:860px){
     .pl-src-row,.pl-manage-row{flex-wrap:wrap;align-items:center;row-gap:10px}
     .pl-manage-row .mmeta,.pl-src-row .mmeta{flex:1 1 200px;min-width:0}
     .pl-manage-row .mact,.pl-src-row .mact{margin-left:auto;justify-content:flex-end;flex-wrap:wrap}
     .pl-manage-row .mst,.pl-src-row .mst{white-space:normal;line-height:1.45;
       overflow-wrap:anywhere;word-break:break-word}
   }
   ```

2. 描述去术语化，改成纯中文短句（后端 `_admin_playlists_payload`）：

   - `source-native` → 「每日自动生成的推荐歌单，曲目取自各音源榜单。」
   - `llm-fallback`  → 「每日自动生成的推荐歌单，曲目由 AI 挑选。」

   内置行右侧的「模式 source-native」同步改为中文 `mode_label`：
   「榜单直取」/「AI 兜底」（`mode` 字段保留，兼容旧前端）。

3. 兜底：`.content` 在窗口很矮时允许整块滚动，音源页新增的面板不会被裁掉。

---

## 验收清单（NAS，34/34）

- v81 回归：本地播放 `Content-Length` 透传、响应体一致（206/206）
- 设置页：`captureScroll` / `restoreScroll` / `#setWrap` 到位
- 重复项：`playlist` 组与 `FNMUSIC_SOURCE_ORDER` 在设置页隐藏；音源页总开关已挂载
- 手机端：`.mact` 三处模板、`flex-wrap`、名称区最小宽度、描述可断行
- 每日推荐：描述中文短句、无 `source-native` / `LLM`、`mode_label` 为中文、`mode` 保留
- 接口：`/admin/api/config` 读写往返（true→false→true）并落盘 `.env`、
  `/admin/api/settings/storage`、`/admin/api/playlists`、`/healthz`
- 喜马拉雅 2070 集回归（v79）通过；服务 `active`

## 回滚

```bash
cd ~/fnmusic_ext
ls proxy/*.bak.v82-*            # 部署前的 app.py / admin_ui.html
sudo cp <备份> proxy/app.py     # 或 proxy/admin_ui.html
sudo systemctl restart fnmusic-ext
```
