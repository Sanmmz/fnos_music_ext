# fnmusic-ext v77 / v77b 变更说明

日期：2026-09-22
目标：① 小说（喜马拉雅专辑）搜索结果不再缺海报 ② 管理页「歌单综合搜索」升级为「综合搜索」，
并把喜马拉雅小说源纳入，搜索栏左侧新增「歌单 / 小说」切换。

---

## 一、问题 1：小说的搜索结果没有海报封面

### 根因

v76 里喜马拉雅歌单记录的 `coverId` 被直接填成了外链图片地址：

```
coverId = "https://imagev2.xmcdn.com/storages/..../xxx.jpg"
```

App 的请求链路是：`coverId` → `GET /music/api/v1/static/cover?guid=<coverId>&size=600`。
代理拿到一个 **不是在线 guid** 的字符串，解析不出音源，既拿不到封面也走不通兜底，
最后落到占位图 / 上游 ⇒ **歌单卡片整块海报空白**。

> ★★ 铁律：`coverId` 必须是 `/static/cover` **能解析的 guid**（如每日推荐那样 `coverId = guid`），
> 绝不能填外链 URL。这条同样适用于以后接入的任何新音源。

### 修复（v77）

| 位置 | 改动 |
|---|---|
| 新增 `_xmly_playlist_cover(album_id)` | 取专辑封面；拿不到时退回第一集封面；返回值恒为绝对 https |
| 新增 `_warm_xmly_poster(pguid, cover_url)` | 先把封面写进 meta 缓存，再走既有 `_prefetch_online_covers` 预热 600 / 300 两档，避免客户端来拉时才现抓（手机端会超时变灰块） |
| `_xmly_album_to_playlist_record()` | `coverId` 由外链改为 `online:playlist:xmly:<albumId>`（= 歌单 guid），并在有封面时预热 |
| `/music/api/v1/search/playlist` | 同上，搜索结果也用歌单 guid + 预热 |
| `/music/api/v1/static/cover` | 在 `_online_info` **之前**插入 `is_xmly_playlist_guid` 分支：meta 缓存 → `_xmly_playlist_cover` → 抓图 → 缓存 → 兜底档位 → 占位 PNG。**绝不 404、绝不返回 JSON**（否则手机端整列裂图） |
| `_user_public_fields()` / `_build_user_bundle()` | 喜马拉雅用户歌单（`我的歌单` 里那一条）的 `coverId` 也指向专辑歌单 guid，不再退化成取不到图的 `online:playlist:user:<pid>` |
| `_admin_playlists_post` 的 `add` | 支持并持久化 `cover` 字段；前端 `addToCur()` 加入时带上封面，列表可直接预热 |

### 修复（v77b）：海报体积

喜马拉雅图床 `imagev2.xmcdn.com` 支持 `!op_type=3&columns=N&rows=N` 现算缩略图。
`_sized_cover_url()` 新增 `xmcdn.com` 分支后，同一张专辑封面：

| 档位 | 字节数 |
|---|---|
| 原图（专辑详情接口返回） | 686 528 B jpg |
| `size=600` | 47 224 B jpg |
| `size=300` | 15 082 B jpg |

注意事项（已写进代码注释）：

- 不要加 `magick=png`：600 档会从 47KB 膨胀到 492KB。
- URL 上已经有 `!...` 时（搜索接口返回的就是 `!op_type=3&columns=290&rows=290&magick=png`）
  **必须先剥掉再拼**，两段 `!` 会被图床判为非法 → `400 Bad Request`。

---

## 二、问题 2：综合搜索 + 歌单/小说 切换

### 后端

- `_admin_playlists_search(keyword, limit, kind)` 新增 `kind` 参数：
  - `kind=playlist`（默认）：网易云 + 酷我，行为与之前完全一致（不传 `kind` 时向后兼容）。
  - `kind=novel`：**只查喜马拉雅**，返回 `source=xmly / source_name=喜马拉雅 / id / name /
    creator / tracks(集数) / cover / category / intro`。
  - 返回体里带 `kind`，前端据此渲染。
- 路由 `/admin/api/playlists/search` 读取 `kind`（兼容别名 `type`）并透传。
- ★ `kind=novel` 走**同步** `_xmly_http()`，不用 `asyncio.run` —— 管理台 HTTP 服务跑在
  socketserver 线程里，用 `asyncio.run` 行为不一致。

### 前端（`proxy/admin_ui.html`）

- 分组标题：`歌单综合搜索` → **`综合搜索`**，副标题 `歌单 / 小说`。
- 新增 `.pl-kind` 分段控件，**渲染在搜索栏内部、输入框左侧**（DOM 顺序 `pl-kind` →
  `input` → `搜索` 按钮）。
- `setPlKind(kind)` 只做局部更新：切高亮、换 placeholder、换说明文案、重绘结果区；
  **不整页重渲染**，否则输入框会失焦、已输入的关键字会丢。
- 文案随类型变化：单位「首 / 集」、结果行显示分类与简介、placeholder 示例
  （歌单：周杰伦 / 华语 / 说唱；小说：吞噬星空 / 三体 / 明朝那些事）。
- 来源徽标新增 `xmly: 🎙️ 喜马拉雅`。
- 移动端 `@media(max-width:860px)`：整条搜索栏允许换行，分段按钮与输入框 `min-height:44px`、
  字号 16px（防 iOS 自动缩放），避免「选择器 + 输入框 + 按钮」挤在一行把输入框压窄。

---

## 三、验收结果

| 脚本 | 结果 |
|---|---|
| `xm_e2e77b.py` | **23 / 24**（唯一 FAIL 是断言把注释里残留的旧名也算进去了，已单独核对页面文案：`label:"综合搜索", sub:"歌单 / 小说"`） |
| `xm_covercold.py`（清缓存冷启动） | **5 / 5**（300=15 082 B、600=47 224 B，二次请求命中缓存且字节一致） |
| `xm_auth_test.py`（真实 token） | **6 / 6**（加入「吞噬星空」→ 1305 集、guid 全部 `online:xmly:*`、标题非空、`206 audio/mp4` 可播） |
| 回归冒烟 | 曲库搜索「小说 三体」20 条全部 `source=xmly`；服务 `active`；日志无 Traceback/ERROR |

关键实测数据：

- `kind=novel` 搜索「吞噬星空」→ 20 条，全部喜马拉雅，均带 https 封面与集数。
- `kind=playlist` 搜索「周杰伦」→ 40 条，源为 `netease` + `kuwo`，**不含** xmly。
- 不传 `kind` → 默认 `playlist`（向后兼容）。
- `/static/cover?guid=online:playlist:xmly:54079657&size=600` → `200 image/jpeg 47224 B`。
- 不存在的专辑 → 占位 PNG **200**，不 404。
- 加入「当前歌单」后，`我的歌单` 列表里该小说的 `coverId = online:playlist:xmly:<albumId>`，
  能取到真实海报；歌单内可取到全部章节并可播放。

---

## 四、回滚

```bash
# 最近一次备份
ls -la ~/fnmusic_ext/proxy/app.py.bak.v77b-* ~/fnmusic_ext/proxy/admin_ui.html.bak.v77b-*
TS=<上面看到的时间戳>
sudo cp -a ~/fnmusic_ext/proxy/app.py.bak.v77b-$TS        ~/fnmusic_ext/proxy/app.py
sudo cp -a ~/fnmusic_ext/proxy/admin_ui.html.bak.v77b-$TS ~/fnmusic_ext/proxy/admin_ui.html
sudo systemctl restart fnmusic-ext
sudo systemctl is-active fnmusic-ext
```

> 再往前一层备份是 `proxy/app.py.bak.v77-<TS>` / `proxy/admin_ui.html.bak.v77-<TS>`（本次修改前）。

---

## 五、当前线上状态

- `proxy/app.py` 400 308 B（v77b）
- `proxy/admin_ui.html` 66 532 B（v77b）
- 服务 `fnmusic-ext`：**active**
- 容器 `fnmusic-xmly`：Up (healthy)；`fnmusic-musicbox` / `fnmusic-musicdl` / `fnmusic-lxmusic` 均正常
- 「当前歌单」里现存一条喜马拉雅小说（`三体|有声书…`，313 集）—— 若为测试残留，可在管理页直接删除。
